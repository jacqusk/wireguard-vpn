# systemd Units

This directory is reserved for tracked systemd unit templates and related notes.

If systemd units are externalized from bootstrap scripts later, store their tracked forms here before installation to `/etc/systemd/system`.