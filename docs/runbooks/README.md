# Runbooks

This directory is reserved for operational runbooks such as:

- incident response,
- peer rotation,
- key rotation,
- rollback and recovery,
- service troubleshooting.

Operational procedures can remain primarily in Polish.

Live deployment snapshots that contain real endpoints, peer profiles, or other sensitive runtime values should stay in local gitignored files under `generated/*.local.md`, not in tracked runbooks.